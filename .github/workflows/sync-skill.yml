name: Sync SKILL.md and rules

on:
  push:
    branches: [main]
    paths:
      - skills/shell/SKILL.md
      - rules/shell-activate.md
      - skills/shell-compress/SKILL.md
      - skills/shell-compress/scripts/**

concurrency:
  group: sync-skill
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Pull latest before making changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main

      - name: Sync SKILL.md copies
        run: |
          cp skills/shell/SKILL.md plugins/shell/skills/shell/SKILL.md

      - name: Sync compress skill to plugin
        run: |
          # Copy scripts verbatim
          rm -rf plugins/shell/skills/compress/scripts
          cp -r skills/shell-compress/scripts plugins/shell/skills/compress/scripts
          rm -rf plugins/shell/skills/compress/scripts/__pycache__

      - name: Rebuild shell.skill ZIP
        run: cd skills && zip -r ../shell.skill shell/

      - name: Sync auto-activation rules
        run: |
          BODY="rules/shell-activate.md"

          # Copilot — no frontmatter, direct copy
          mkdir -p .github
          cp "$BODY" .github/copilot-instructions.md

      - name: Commit and push if changed
        run: |
          git add \
            plugins/shell/ \
            shell.skill \
            .github/copilot-instructions.md
          git diff --staged --quiet && exit 0
          git commit -m "chore: sync S.H.E.L.L. SKILL.md copies and auto-activation rules [skip ci]"
          git push
